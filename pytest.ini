[pytest]
addopts = -v
testpaths = tests
pythonpath = . setup sources slack scrapers status debuggers login
markers =
    integration: end-to-end integration tests (may be slow or require network)
filterwarnings =
    ignore::RuntimeWarning