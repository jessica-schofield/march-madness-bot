[pytest]
addopts = -v
testpaths = tests
pythonpath = . setup sources slack scrapers status debuggers login
markers =
    integration: end-to-end tests that exercise run_setup with mocked I/O
    unit: fast isolated tests of individual functions
filterwarnings =
    ignore::RuntimeWarning